{
  "name": "mzda-kalkulacka",
  "private": true,
  "version": "2026.1.0",
  "type": "module",
  "description": "Mobilní PWA kalkulačka čisté mzdy HPP a OSVČ IT — ČR 2026",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "test": "node --test tests/*.test.js",
    "icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
